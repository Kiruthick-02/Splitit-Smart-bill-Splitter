module.exports = {
  plugins: {
    // The tailwindcss plugin is required to process Tailwind's syntax.
    tailwindcss: {},

    // The autoprefixer plugin adds vendor prefixes to CSS rules for
    // better browser support.
    autoprefixer: {},
  },
};